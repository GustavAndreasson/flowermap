<form name="add_plant" class="pop-up" action="controllers/plant.php" method="POST" enctype="multipart/form-data" style="display:none;">
  <input type="hidden" name="action" value="add_plant" />
  <input type="hidden" name="coord_x" />
  <input type="hidden" name="coord_y" />
  <h2><?= $T->__("Add a plant") ?></h2>
  <div class="plant">
    <div class="select" id="slct_species">
      <input type="hidden" name="species_id" />
      <div class="option selected" data-value="null"><?= $T->__("New species") ?></div>
      <?php foreach($fm->user->garden->species as $id => $species): ?>
        <div class="option" data-value="<?= $id ?>"><?= $species->get_name() ?></div>
      <?php endforeach; ?>
    </div>
    <div class="row"><label for="add_plant_description"><?= $T->__("Description") ?></label><input type="textarea" name="description" id="add_plant_description"></div>
    <div class="row"><label for="add_plant_image"><?= $T->__("Image") ?></label><input type="file" accept="image/jpeg" name="image" id="add_plant_image"></div>
  </div>
  <div class="buttons">
    <button type="button" class="cancel"><?= $T->__("Cancel") ?></button>
    <button type="submit"><?= $T->__("Add species") ?></button>
  </div>
</form>
