<div class="header">
  <h1>
    <?= $T->__("Flower Map"); ?>
    <?php if($fm->is_logged_in()): ?>
      <?php if($fm->user->garden->get_name()): ?>
        - <span id = "garden_name"><?= $fm->user->garden->get_name(); ?></span>
      <?php endif; ?>
    <?php endif; ?>
  </h1>
  <?php if($fm->is_logged_in()): ?>
    <form name="logout" action="user/logout" method="POST">
      <button type="submit"><?= $T->__("Logout") ?></button>
    </form>
    <button type="button" id="btn_edit_garden"><?= $T->__("Garden settings") ?></button>
  <?php endif; ?>
</div>
